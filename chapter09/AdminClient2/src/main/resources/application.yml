server:
  port: 8081
spring:
  application:
    name: adminClient2
  boot:
    admin:
      client:
        url: http://localhost:8080
management:
  endpoints:
    web:
      exposure:
        include: "*"